// Stub module for @react-native-firebase/* packages.
// Used by Metro resolver to prevent crashes in Expo Go where
// native Firebase modules (RNFBAppModule, etc.) are not available.
//
// In dev builds / production, the real Firebase packages are used
// because EX_FIREBASE=1 is set, which tells metro.config.js to skip
// the stub and resolve the real @react-native-firebase packages.

module.exports = {};
